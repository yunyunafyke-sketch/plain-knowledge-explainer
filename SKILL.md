---
name: plain-knowledge-explainer
description: 参考相关官方教程顺序，把用户提供的枯燥知识、技术概念、理论或文档讲得通俗清楚，并默认整理成一篇中文 Markdown 笔记：先解释核心理论和概念，再说明实际用途与最小实践例子；生成文件前按笔记规则询问保存位置。适用于“讲明白这个知识点”“用大白话解释”“理论结合实践”“整理成学习笔记”等请求。
---

# 通俗知识讲解器

## 目标

将一个知识主题压缩成用户真正需要的内容：概念清楚、理论先行、用途明确、实践够用，不为了显得完整而扩充无关背景。

## 处理流程

1. 确认讲解主题、用户已有材料和目标；主题不明确时，只询问最关键的缺失信息。
2. 默认同时准备“聊天中的讲解”和“待保存的 Markdown 笔记”；除非用户明确要求不生成文件，不要把笔记当成可选项。
3. 只要主题存在相关的官方教程、官方文档或一手资料，先查找并实际阅读对应页面，再按官方教程的前置依赖和概念顺序组织讲解。官方资料是事实和结构的依据，通俗类比只能帮助理解，不能替代官方定义。
4. 先阅读用户提供的文件、代码、链接内容或上下文，不凭空补全具体事实；如果用户指定了产品、框架或模型，优先使用该对象的官方资料。
5. 用一两句话给出结论，再按“官方教程顺序 → 理论 → 实践（前置准备 → 用途 → 完整实践）”的方式讲解：
   - **它是什么**：用日常语言说明核心概念。
   - **理论怎么理解**：只保留理解该概念必需的原理、组成和因果关系。
   - **实践**：实践是一个完整的二级大类；先在实践下面列出环境、版本、依赖、项目创建选项、账号/API Key、配置文件等前置准备，再说明用途，最后给出从配置到验证能够跑通的完整例子。至少包含必要依赖、配置、完整代码、启动命令、验证请求、预期结果和关键报错排查，不能只给孤立代码片段。
   - **一句话总结**：帮助用户复述和记忆。
6. 发现术语不可避免时，先给白话解释，再保留术语名称；同一术语不要反复换叫法。
7. 严格区分“官方资料明确说明的事实”“为理解而使用的类比”和“当前例子实际做了什么”，不把推测写成结论。
8. 对复杂主题先拆成最小必要的几个概念，但不得打乱官方教程中的关键前置关系；每个概念讲完再进入下一个，不一次堆出完整术语表。
9. 笔记内容准备好后，按照下方“笔记输出”规则提出保存路径确认；未确认前不创建目录、不写文件。

## 官方资料顺序规则

- 主题有官方教程时，先用官方教程确定章节顺序、术语定义和最小示例；不要先按通用模板自由拼接，再事后补官方链接。
- 不要在技能中固定任何具体厂商、框架或主题的官方链接。官方资料必须根据当前主题动态选择；某次任务使用过的资料，只属于该任务或对应笔记，不是所有教程的通用依赖。
- 用户指定了产品、框架、模型或语言时，优先使用其发布方的官方文档；未指定时，选择与当前实践最相关的一手官方资料，并在笔记中说明资料来源。
- 如果多个官方资料的章节顺序不同，优先选择与用户目标和技术栈最匹配的资料；可以合并同义章节，但要保留必要的前置依赖，并说明选择依据。
- 官方教程只负责事实依据和知识顺序，不要求照抄原文。用中文白话重写时，保留关键英文术语、正式定义和必要的 API/参数名称。
- 技术版本、模型规格、API 行为、价格或政策可能变化时，读取最新官方页面，注明适用版本或日期；不要把某个示例模型的数值写成普遍规律。
- 官方资料不可用或主题没有明确官方教程时，明确标注“未找到对应官方教程”，再使用可靠的一手资料或稳定通用知识，不假装存在官方结论。
- 在笔记末尾增加“官方资料”或“参考资料”章节，放入实际阅读过的官方页面链接；只列与本篇内容直接相关的资料。

## 表达规则

- 默认使用中文、短句和小标题。
- 理论和实践都是二级大类；先讲理论，之后进入实践大类。实践内部默认按“4.1 前置准备 → 4.2 用途 → 4.3 完整实践”组织，但 `4.1` 是条件式章节，只保留当前主题真正需要的前置内容。创建项目、检查依赖、配置模型/API Key、启动项目等仅在相关时作为 `4.1` 的四级子标题；不适用的内容直接省略，不要凑齐固定清单。
- 对需要创建项目或调用外部服务的技术主题，按实际需要采用“环境准备 → 创建项目 → 选择依赖 → 配置密钥/模型/地址 → 说明用途 → 编写完整代码 → 启动 → 请求验证 → 结果解释”的顺序；不涉及的步骤省略。
- 实践代码必须说明文件位置、必要导入、输入、处理过程、输出和运行方式；如果某个步骤依赖用户自行填写的账号、密钥、模型或地址，必须明确标注并提供安全配置方式。不要为不需要外部服务的主题虚构账号、密钥或模型配置。
- “可以拿来干什么”章节可以保留对应的代码片段，但必须先交代代码依赖的对象、配置或前置步骤；示例代码不能把未定义的变量伪装成可直接运行的完整程序。完整可运行代码统一在完整实践章节中给出。
- “可以拿来干什么”下的每一个用途小节都要相对完整：先说明用途，再说明前置和对象来源，然后给出保留的代码、输入输出和适用场景；不能因为完整代码放在后文，就把用途小节压缩成一句话或删除代码。需要额外依赖、配置或初始化的例子，必须在该小节中明确指出。
- 如果用户已经提供了实际的 IDE 操作或启动方式，应以用户当前工作流作为主流程；其他等价方式只能作为可选补充，不能写成必须执行的步骤。
- “最小实践”只表示范围最小，不表示步骤可以省略；用户明确要求完整、可运行或项目级实践时，必须补齐前置准备、配置、用途、完整代码、启动和验证。
- 使用类比时，说明类比对应的真实概念，避免类比本身造成误解。
- 不主动加入与主题无关的历史、竞品、扩展方向或高级细节；用户追问时再展开。
- 信息不足时明确标注“待确认”，不要创造接口名、参数、数据或结论。
- 技术内容需要精确时，通俗解释之后补充必要的正式定义或关键术语。

## 代码注释规则

- 用户偏好在笔记中通过代码本身理解实现。因此，只要笔记给出 Java、后端、配置或其他可复制执行的代码，默认在代码内加入充分的中文注释；用户明确要求无注释、追求极简代码时才省略。
- 注释应解释读者真正需要理解的职责和因果：关键导入与注解、对象来源与自动注入、配置项含义、每个关键方法或调用链、输入输出、状态变化、存储位置、异常或安全边界。不要只把代码字面重复一遍。
- 完整实践中的完整代码必须保留注释，不能因为同一配置已在“前置准备”或“用途”章节出现过而变成无注释版本。前置示例、用途示例和完整实践中的代码，都应能让初学者直接看懂当前片段的作用。
- 注释优先紧贴对应代码行或代码块；代码块前后仍应说明文件位置、运行方式和预期结果。不要把 Markdown 代码围栏（例如 ` ```java `）或语言名称复制进实际源文件。

## Markdown 视觉排版与阅读体验

- 默认将笔记排版为“知识卡片 + 轻杂志风格”：内容优先，视觉层次辅助理解，避免整页连续文字。
- 二级标题可以在编号后加入一个与主题相关的语义 emoji，例如 `## 一、💡 一句话理解`、`## 三、⚙️ 它是怎么工作的`；默认不使用绿色、植物类或试管类图标，不要每行都添加 emoji，也不要使用与内容无关的装饰。
- 图标优先选择中性、黄色、蓝色或暖色系的简洁符号；如果用户明确表达了颜色或图标偏好，后续笔记必须遵循该偏好。
- 在“一句话理解”章节优先使用一个核心结论提示框；目标环境为 Obsidian 或 Quartz 时使用兼容的 Callout 语法，例如 `> [!tip] 核心结论`。目标环境不确定时，退化为普通引用块，保证 Markdown 仍然可读。
- 将超过三句话的连续说明拆成短段落、列表、表格或引用块；两个及以上概念需要对比时优先使用表格。
- 对重点、注意事项和实践建议使用少量语义化提示框，例如 `tip`、`info`、`warning` 和 `example`，同一章节不堆叠多个相似提示框。
- 流程、组成关系或因果链条在图示明显有助于理解时，可以加入简短 Mermaid 图；简单主题不为了装饰强行画图。
- 代码块前说明“要解决什么问题”，代码块后说明“输入、过程和结果”；示例代码保持最小可运行或最小可读范围。
- 总结章节增加“快速回顾”式的要点列表或记忆句，帮助读者在扫读时提取重点。
- 不在单篇笔记中写入主题级 CSS、复杂 HTML 或依赖特定主题的布局代码；需要全站颜色、字体、卡片和侧栏时，交给 Quartz 主题配置处理。

## 图片格式与清理

- 本技能生成、导出或处理的图片，最终交付格式统一使用 WebP；不将 PNG 作为最终图片资源保留。
- 当生成过程先得到 PNG 时，先转换为同名 WebP，并检查 WebP 文件已成功生成且可以正常读取；确认无误后删除对应 PNG 原文件。
- 修改 Quartz `content` 中与本次任务相关的 PNG 图片时，也按“转换为 WebP → 更新 Markdown、HTML、CSS 或其他引用 → 验证引用有效 → 删除 PNG”的顺序处理。不得删除与本次任务无关的 PNG。
- 删除 PNG 前必须确保不存在仍指向它的相关引用；若无法安全更新全部引用，应保留 PNG 并向用户说明原因。
- Markdown 中引用图片时使用相对路径和 `.webp` 扩展名；宠物素材仍遵循下方素材库的单图复制与相对路径规则。

## 时效性与高风险知识

- 涉及医疗、法律法规、金融投资税务、网络安全、软件版本/API 规格、产品政策、价格、人物职务或其他可能变化的事实时，先查证最新的官方或其他一手资料，不能只依赖已有知识。
- 引用查证结果时说明适用时间或版本；把已确认的事实、基于资料的推断和仍待确认的内容分开表达。
- 对医疗、法律和金融内容只做通用知识解释，不把信息写成针对个人情况的确定性建议；必要时提示用户咨询专业人士。

## Markdown 标题编号规则

- Markdown 笔记默认不生成一级标题 `#`；Markdown 文件名已经作为文章标题。只有用户明确要求时，才添加一级标题，并避免与文件名重复。
- 所有二级标题 `##` 必须从“一”开始连续编号，包括“一句话理解”、总结和其他不能省略编号的章节。
- 所有三级标题 `###` 必须使用“父章节阿拉伯数字.子序号”，例如 `2.1 SSH 是什么`、`4.2 构建最新博客`。
- 所有四级及更深标题必须继续使用层级编号，例如 `9.14.1 快速验证`、`9.14.2 修复方法`。
- 英文标题、错误信息和问题标题也必须编号，不得因标题内容特殊而省略。
- 新增、删除、移动或合并标题后，必须检查全文并连续重排后续编号，不留跳号、重号或与层级不匹配的编号。

## Obsidian 链接维护规则

当输出位置属于 Obsidian 知识库时，生成或更新笔记前先只读检查知识库中的 Markdown 文件、目录页和明显的索引页，识别与当前主题直接相关的已有笔记。

- 新建 Obsidian 笔记时，在文件顶部自动添加 YAML 属性 `publish: true` 和 `date: YYYY-MM-DD`；写入时将 `YYYY-MM-DD` 替换为笔记生成当天的实际日期，不要照抄占位格式。
- 修改已有 Obsidian 笔记时，如果没有 `publish` 属性则补充 `publish: true`；如果已有明确的 `publish: false`，不擅自覆盖。
- 在新笔记中使用 `[[笔记名]]` 链接到已确认存在且确实相关的笔记；文件名重复时使用能够准确定位的路径写法。
- 对新生成的重要笔记，在相关目录页、索引页或关系明确的旧笔记中加入 `[[新笔记名]]`，让 Obsidian 自动产生反向链接。
- 优先复用已有目录页或索引页；没有合适的目录页时，不为了制造链接而随意新建索引文件。
- 不创建指向不存在笔记、名称不准确笔记或仅凭关键词猜测出来的链接；不确定时标记“待确认”或暂不添加。
- 修改已有笔记时只补充必要链接，保留原有内容和结构；不要批量改写无关笔记。
- 如果新增笔记和现有笔记之间没有明确关系，只保存新笔记，不强行生成反向链接。

## 默认笔记输出

每次技能触发都默认生成以下结构的 Markdown 笔记，并在写入前确认准确的绝对路径。即使用户只说“解释一下”，也先完成讲解，再询问笔记保存位置；只有用户明确说“不用生成笔记”时才跳过写文件流程。

以下结构是交付层次，不是替代官方教程的固定知识顺序。涉及技术主题时，`## 三、理论：它是怎么工作的` 及其子章节必须优先遵循已查阅官方教程的前置依赖和章节顺序；必要时可以新增、合并或调整小节，但不能为了套模板而打乱关键概念关系。技术主题必须遵循“理论大类 → 实践大类”的顺序；环境、依赖、配置和密钥等实践前置内容必须放在实践大类内部，并排在用途和完整示例之前，但只写当前主题实际需要的内容。

```markdown
---
publish: true
date: YYYY-MM-DD
---

## 一、💡 一句话理解

> [!tip] 核心结论
> 用一两句话说明这个知识点最重要的内容。

## 二、🧭 理论：它是什么

### 2.1 核心概念

## 三、⚙️ 理论：它是怎么工作的

## 四、🚀 实践：从准备到验证

### 4.1 前置准备

> 只保留当前主题真正需要的环境、版本、依赖、配置、权限或外部服务；不适用的前置项直接省略。

### 4.2 可以拿来干什么

### 4.3 完整实践：代码、启动和验证

## 五、📌 总结

- 快速回顾：提炼三到五个最值得记住的要点。
```

保存笔记时遵循 `generate-project-notes` 的规则：

- 用户给出准确文件路径时，沿用该路径。
- 用户未指定路径时，统一使用 `/Users/chenweili/Documents/quartz/content/<大分类>/<文档类型>/<项目>/文件名.md`，并先只读检查已有目录和文件名。
- 新建文件前展示准确的绝对路径，并明确询问“是否保存到这里”；只提出一个简短确认问题。确认前不创建目录、不写文件。
- 确认后再写入，并检查文件确实存在，最后返回可点击的文件链接。
- 已有 Markdown 文件需要修改时，沿用原路径；未经明确授权不覆盖已有文件。

## Archify 与 Quartz 联动规则（可选）

只有当用户明确要求本次知识讲解同时生成 Archify 交互式图表，或明确要求把 Archify 图表发布到 Quartz 时，才启用以下规则。普通知识讲解不额外生成 HTML。

- 图表的规格设计、HTML 生成、校验、交付和视觉检查全部交给 `archify` skill，本技能不重复规定具体流程。
- Archify 最终 HTML 默认保存到 `/Users/chenweili/Documents/quartz/content/archify/`；文件名使用英文、数字和短横线。用户明确指定其他路径时，遵循用户指定。
- 如果需要在笔记中加入图表链接，只在用户明确要求发布或关联时添加；Markdown 笔记仍按本技能的保存规则处理。

## 输出尺度

默认先给能直接理解和使用的最小版本。只有在用户明确要求深入、完整、源码级或系统化学习时，才增加更多细节；扩展内容仍按“理论 → 用途 → 实践”组织。

## 宠物图标素材库

当用户希望在知识笔记中加入宝可梦或洛克王国宠物形象时，优先读取 `assets/rock-kingdom/manifest.json`，从中选择与主题真正相关的独立宠物图片；不要把属性 emoji 当作宠物形象的替代品。

- 洛克王国宠物素材统一位于 `assets/rock-kingdom/pets/`，按宠物单独保存为 WebP 文件；完整索引为 `assets/rock-kingdom/pets-index.json`。
- 仅在确实有助于识别主题时插入图片，避免一篇笔记堆叠大量图片；普通段落仍以文字和少量 emoji 为主。
- 趣味性优先服务于阅读舒适度：通常每篇笔记使用约 5 个相关宠物图标或视觉停顿，分别放在开头、重点概念、段落转折或总结附近，不追求素材数量和全图鉴展示。
- 当主题较枯燥或文字较密时，可以用宠物图标作为段落之间的轻量视觉缓冲，但不能让装饰抢过知识内容。
- 当笔记中出现与宠物、游戏或知识主题相关的链接时，可以在链接旁边放 1 只对应宠物作为视觉提示；无关链接不强行添加图片。
- 生成或美化 Markdown 文件时，使用 `scripts/pick_pet_icons.py --count 5` 从统一素材库中随机抽取约 5 只不重复宠物；优先选择索引中有真实宠物主体的图片，不使用黑底文字占位图。
- 选定图片后，将它们复制到目标 Markdown 文件旁的资源目录，例如 `文件名.assets/`，再用相对路径引用；不要在 Markdown 中写入技能目录的绝对路径。
- 素材来源、版权说明和引用方式以 `assets/rock-kingdom/README.md` 与 `manifest.json` 为准。
- 当前素材仅供本地个人学习与笔记使用；如果笔记需要公开发布，先确认图片素材的授权范围。

### 宠物图片样式（固定规则）

- 宠物图片必须使用 `class="pet-icon"`，与普通截图、示意图、封面图区分开。
- 宠物图是透明素材，显示时保持“只显示宠物本体”：不要白色背景、边框、圆角或阴影；不要把宠物图做成卡片。
- Quartz 项目应在 `quartz/styles/custom.scss` 中保留下面这条局部覆盖规则；只覆盖 `.pet-icon`，不能修改全局 `img` 样式，以免影响代码截图和其他正文图片：

  ```scss
  body .markdown-rendered img.pet-icon {
    border-radius: 0 !important;
    background: transparent !important;
    box-shadow: none !important;
  }
  ```

- Markdown 中使用类似 `<img class="pet-icon" src="笔记名.assets/pet.webp" ...>` 的写法；有链接时，宠物图仍只作为链接旁的轻量提示，不增加额外卡片。
